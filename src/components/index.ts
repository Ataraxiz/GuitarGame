/**
 * Component Barrel Export
 *
 * Centralizes component exports for cleaner imports throughout the app.
 * Instead of: import { NotePlayer } from './components/NotePlayer'
 * We can use: import { NotePlayer } from './components'
 */
export { NotePlayer } from './NotePlayer'
export { GuitarGame } from './GuitarGame'

